--Query--
SELECT * EXCLUDE (secret_string) FROM duckdb_secrets() ORDER BY name
--Result--
[('temp_s1', 's3', 'config', False, 'memory', ['s3://', 's3n://', 's3a://']), ('temp_s2', 's3', 'config', False, 'memory', ['s3://', 's3n://', 's3a://'])]
+--------------------+
--Query--
SELECT * EXCLUDE (secret_string) FROM duckdb_secrets() ORDER BY name
--Result--
[('temp_s1', 's3', 'config', False, 'memory', ['s3://', 's3n://', 's3a://']), ('temp_s2', 's3', 'config', False, 'memory', ['s3://', 's3n://', 's3a://'])]
+--------------------+

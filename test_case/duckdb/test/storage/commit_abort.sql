SELECT COUNT(*), COUNT(a), COUNT(b), SUM(a), SUM(b), SUM(LENGTH(c)) FROM test;
SELECT * FROM test ORDER BY a, b, c;
SELECT * FROM test ORDER BY a, b, c;
SELECT COUNT(*), COUNT(a), COUNT(b), SUM(a), SUM(b), SUM(LENGTH(c)) FROM test;
SELECT COUNT(*), COUNT(a), COUNT(b), SUM(a), SUM(b), SUM(LENGTH(c)) FROM test;
SELECT * FROM test ORDER BY a, b, c;

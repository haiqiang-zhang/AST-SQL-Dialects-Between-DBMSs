select constraint_text from duckdb_constraints() where constraint_type = 'PRIMARY KEY';
select constraint_text from duckdb_constraints() where constraint_type = 'PRIMARY KEY';
select constraint_text from duckdb_constraints() where constraint_type = 'PRIMARY KEY';
select constraint_text from duckdb_constraints() where constraint_type = 'PRIMARY KEY';
select constraint_text from duckdb_constraints() where constraint_type = 'PRIMARY KEY';

SELECT DISTINCT a FROM test ORDER BY a;
SELECT DISTINCT a FROM test ORDER BY a;
SELECT DISTINCT a FROM test ORDER BY a;
SELECT DISTINCT a FROM test ORDER BY a;
SELECT DISTINCT a FROM test ORDER BY a;
SELECT DISTINCT a FROM test ORDER BY a;
SELECT DISTINCT a FROM test ORDER BY a;
SELECT DISTINCT a FROM test ORDER BY a;

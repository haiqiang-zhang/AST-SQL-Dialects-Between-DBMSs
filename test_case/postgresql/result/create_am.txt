--Query--
SELECT amname, amhandler, amtype FROM pg_am where amtype = 't' ORDER BY 1, 2
--Result--
[('heap', 'heap_tableam_handler', 't')]
+--------------------+
--Query--
SELECT     pc.relkind,     pa.amname,     CASE WHEN relkind = 't' THEN         (SELECT 'toast for ' || relname::regclass FROM pg_class pcm WHERE pcm.reltoastrelid = pc.oid)     ELSE         relname::regclass::text     END COLLATE "C" AS relname FROM pg_class AS pc,     pg_am AS pa WHERE pa.oid = pc.relam    AND pa.amname = 'heap2' ORDER BY 3, 1, 2
--Result--
[]
+--------------------+
--Query--
SELECT pg_describe_object(classid,objid,objsubid) AS obj FROM pg_depend, pg_am WHERE pg_depend.refclassid = 'pg_am'::regclass     AND pg_am.oid = pg_depend.refobjid     AND pg_am.amname = 'heap2' ORDER BY classid, objid, objsubid
--Result--
[]
+--------------------+
--Query--
SELECT amname FROM pg_class c, pg_am am   WHERE c.relam = am.oid AND c.oid = 'heaptable'::regclass
--Result--
[('heap',)]
+--------------------+
--Query--
SELECT pg_describe_object(classid, objid, objsubid) as obj,        pg_describe_object(refclassid, refobjid, refobjsubid) as objref,        deptype   FROM pg_depend   WHERE classid = 'pg_class'::regclass AND         objid = 'heaptable'::regclass   ORDER BY 1, 2
--Result--
[('table heaptable', 'schema public', 'n')]
+--------------------+
--Query--
SELECT pg_describe_object(classid, objid, objsubid) as obj,        pg_describe_object(refclassid, refobjid, refobjsubid) as objref,        deptype   FROM pg_depend   WHERE classid = 'pg_class'::regclass AND         objid = 'heaptable'::regclass   ORDER BY 1, 2
--Result--
[('table heaptable', 'schema public', 'n')]
+--------------------+
--Query--
SELECT amname FROM pg_class c, pg_am am   WHERE c.relam = am.oid AND c.oid = 'heaptable'::regclass
--Result--
[('heap',)]
+--------------------+
--Query--
SELECT COUNT(a), COUNT(1) FILTER(WHERE a=1) FROM heaptable
--Result--
[(9, 1)]
+--------------------+
--Query--
SELECT amname FROM pg_class c, pg_am am   WHERE c.relam = am.oid AND c.oid = 'heapmv'::regclass
--Result--
[('heap',)]
+--------------------+
--Query--
SELECT amname FROM pg_class c, pg_am am   WHERE c.relam = am.oid AND c.oid = 'heapmv'::regclass
--Result--
[('heap',)]
+--------------------+
--Query--
SELECT COUNT(a), COUNT(1) FILTER(WHERE a=1) FROM heapmv
--Result--
[(9, 1)]
+--------------------+

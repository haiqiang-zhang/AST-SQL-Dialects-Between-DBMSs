--Query--
SELECT * APPLY x->argMax(x, number) FROM numbers(1)
--Result--
[(0,)]
+--------------------+
--Query--
EXPLAIN SYNTAX SELECT * APPLY x->argMax(x, number) FROM numbers(1)
--Result--
[('SELECT argMax(number, number)',), ('FROM numbers(1)',)]
+--------------------+

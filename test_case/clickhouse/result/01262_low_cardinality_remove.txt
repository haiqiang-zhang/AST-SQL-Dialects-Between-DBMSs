--Query--
SELECT CAST(ALow, 'String') AS AStr FROM testView GROUP BY AStr ORDER BY AStr
--Result--
[('A',), ('B',), ('C',)]
+--------------------+
--Query--
SELECT CAST(ALow, 'String') AS AStr FROM testView GROUP BY AStr ORDER BY AStr
--Result--
[]
+--------------------+
--Query--
SELECT CAST(ALow, 'String') AS AStr FROM testView GROUP BY AStr ORDER BY AStr
--Result--
[('A',), ('B',), ('C',)]
+--------------------+
